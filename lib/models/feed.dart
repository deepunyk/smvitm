class Feed{
  final feedId;
  final categoryId;
  final feedTitle;
  final feedDescription;
  final feedType;
  final feedTime;
  final facultyId;

  Feed({this.feedId, this.categoryId, this.feedTitle, this.feedDescription,
      this.feedType, this.feedTime, this.facultyId});
}